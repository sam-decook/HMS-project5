@model IEnumerable<project5.Models.plancourses>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.courseID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.yearTaken)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.termTaken)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.courseID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.yearTaken)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.termTaken)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.planID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.planID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.planID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
