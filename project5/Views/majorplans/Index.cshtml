@model IEnumerable<project5.Models.majorplan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.planID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.majorID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.planID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.majorID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.userPlanID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.userPlanID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.userPlanID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
